{
  "name": "excalidraw-mcp",
  "version": "0.1.0",
  "description": "MCP server that bridges Claude Code / Claude Desktop to the running Excalidraw Tauri app",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.27.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
